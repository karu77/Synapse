PORT=3000
# Add your production frontend URL here. This is REQUIRED for deployed environments.
# e.g. FRONTEND_URL=https://my-synapse-app.vercel.app
FRONTEND_URL=
GEMINI_API_KEY="your_gemini_api_key_here"
MONGO_URI="your_mongodb_connection_string_here"
JWT_SECRET="a_long_random_secure_string_for_jwt"

# (Highly Recommended for deployed servers)
# YouTube may block requests from cloud hosting IPs. To avoid this, provide a proxy URL.
# Example: http://user:password@proxy.example.com:8080
PROXY_URL="" 